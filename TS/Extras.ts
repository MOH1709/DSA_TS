/**
 * we can set number as type in ts, which is equal to const!!!
 * ? stands in type decalration for type|undefined
 * type alias = number | string;
 * unknown will not allow to assign itself to other type variable like any
 * funtion default return type is void.
 * *  if return type is defined undefined, so we have to compulsory return undefined
 * namespace
 * we cannot run ts file directly on browser
 * bydefault a variable is of any type
 * never is type of function throws error, not return anything
 */
